# Kubernetes Examples

This directory contains simplified and sanitized Kubernetes manifests based on patterns used within my self-hosted infrastructure.

The examples demonstrate common workload patterns such as:

- Deployments
- Services
- Ingress
- Persistent storage
- health checks
- resource limits

These files are intentionally generic and do not contain production configuration, credentials or internal infrastructure information.
